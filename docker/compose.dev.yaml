services:
  engine:
    image: minions-engine:test
    volumes:
      - /home/prei/minions/minions-ui:/hostrepo:ro
      - ${HOME}/.claude:/workspace/home/.claude
      - ${HOME}/.codex:/workspace/home/.codex
    environment:
      DEFAULT_REPO: file:///hostrepo
